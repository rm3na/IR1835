TASK 1
cp /mnt/c/data/ioxclient_1.17.0.0_linux_amd64.tar.gz /home/student

cd /home/student

tar -xvzf ioxclient_1.17.0.0_linux_amd64.tar.gz 

export PATH=$PATH:/home/student/ioxclient_1.17.0.0_linux_amd64/

ioxclient -v


TASK 2
# Add Docker's official GPG key:
sudo apt update
sudo apt install ca-certificates curl
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/debian/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

# Add the repository to Apt sources:
sudo nano /etc/apt/sources.list.d/docker.sources

sudo apt update

sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

sudo newgrp docker 

exit

sudo usermod -a -G docker student

sudo docker --version



TASK 3
sudo apt-get install qemu-user qemu-user-static

ls -al /usr/bin/qemu-*static 


TASK 4
sudo docker run -v /usr/bin/qemu-aarch64-static:/usr/bin/qemu-aarch64-static --rm -ti arm64v8/alpine:3.7

uname -a

exit


TASK 5
mkdir iox_aarch64_webserver 

cd iox_aarch64_webserver

nano webserver.py

nano Dockerfile 

cp /usr/bin/qemu-aarch64-static .


TASK 6
sudo docker buildx build --platform linux/arm64 -t iox_aarch64_webserver .

sudo docker run -ti iox_aarch64_webserver

uname -a 

python webserver.py &

netstat -tlpn

exit


TASK 7
nano package.yaml

sudo docker save -o rootfs.tar iox_aarch64_webserver

sudo chown student:student rootfs.tar

ls

















